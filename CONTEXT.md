# Running Training Dataset

러닝 훈련법을 비교 가능한 형태로 적어두는 카탈로그. 무엇을 처방하는지가 아니라 **무엇이
알려져 있고 무엇이 아닌지**를 기록하는 것이 목적이므로, 용어 대부분이 "이것은 얼마나
확실한가"를 구분하기 위해 존재한다.

## 엔티티

**훈련법**:
브라우징 단위. 하나의 접근법이며 `bet`·실행 조건·전환 비용을 갖는다.
_피할 것_: 체계(직역이라 뻣뻣하다), 프로그램·계획(따라 하는 구체적 일정을 뜻하고, 훈련법은
그 일정을 만들어내는 쪽이다 — [ADR 0006](docs/adr/0006-korean-only-dataset.md) 참조)

**워크아웃**:
세션 하나. 훈련법에 중립적인 id를 갖고, 어떤 훈련법이 그것을 뭐라 부르는지는 여기 없다.
_피할 것_: 운동, 세션

**용례**:
`(훈련법, 워크아웃) → 통칭` 매핑. 이름은 필드가 아니라 조인이라는 이 프로젝트의 핵심
주장을 담는 표.
_피할 것_: 명칭 테이블

**통칭**(`calls_it`):
어떤 훈련법이 그 워크아웃을 실제로 부르는 이름. 워크아웃 행에는 절대 들어가지 않는다.
_피할 것_: 별칭, 이름

**앵커**:
강도를 재는 기준. `daniels-vdot`, `pct_hrmax`처럼 무엇을 읽어 강도를 정하는지를 말한다.
_피할 것_: 강도 모델, 존

**구성개념**:
앵커가 읽는 물리량(지각·페이스·심박·대사). 묶어서 보여줄 뿐 서로 환산되지는 않는다.

**적응**:
워크아웃이 표적으로 삼는다고 **주장되는** 생리 변화. 만들어낸다고 주장하지 않는다.

## 확실성

**주장**(`claim.proposition`):
반증 가능한 한 문장. 행 하나가 무엇을 걸고 있는지.
_피할 것_: 효과, 효능

**반증 절차**(`test`):
그 주장이 틀렸다면 무엇이 관찰될지. 관찰 불가능하면 `detectable: false`로 그렇게 말한다.
_피할 것_: 검증, 측정법

**근거 등급**:
주장이 얼마나 뒷받침되는지. `정설`(consensus) / `유력`(plausible) / `관행`(tradition).
`정설`은 사람이 읽었다는 `verified` 없이는 붙을 수 없다.
_피할 것_: 신뢰도, 근거 수준

**교란**(`confounds`):
같은 관찰을 만들어낼 수 있는 다른 원인. 최소 하나가 강제된다. `shares_mechanism`은 주장과
같은 생리를 통해 작용해서 관찰로 분리 불가능한 최악의 경우.

**출처**(`source`) / **인용**(`cite`):
출처는 그 훈련법이 **무엇을 처방하는지**의 기록, 인용은 그것이 **작동한다**는 근거. 다른
주장이며 한 행에서 같은 저작이 둘 다일 수 없다.
_피할 것_: 참고문헌(둘을 뭉갠다)

**정본 상태**(`provenance`):
`source`가 비어 있을 때 어떤 종류의 빈칸인지. `recorded` / `unrecorded` / `uncitable`.
UI에서는 `정본` / `정본 미기록` / `정본 없음`.

## 전환

**전환 비용**(`switching_cost`):
다른 훈련법에서 이리로 넘어올 때 무엇이 어긋나는지.

**조용함**(`silent: true`):
쓰던 용어가 그대로 살아남으면서 뜻만 바뀌는 경우. 알아채기 어려워서 가장 위험하다. 반대는
**드러남**.
_피할 것_: 암묵적, 무성

## 쓰지 않는 말

**`expected_improvement`**:
"이걸 하면 몇 분 빨라진다". 이 프로젝트의 신뢰를 무너뜨릴 유일한 필드이고, 그래서 `test`가
그 자리에 있다. 한국어로 옮긴 형태("예상 개선", "기대 효과")도 마찬가지다.

**프로그램 / 계획**:
훈련법을 가리키는 데 쓰지 않는다. 위 참조.

## 언어

산문은 한국어다. 영어는 **번역이 아니라 데이터**인 곳에만 남는다 — 통칭, `canonical_name`,
귀속, 인용. 경계는 [ADR 0007](docs/adr/0007-korean-repo-prose.md)이 긋는다.
